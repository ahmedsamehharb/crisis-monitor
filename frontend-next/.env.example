# crisis-monitor Backend (Clustering-API auf Port 3001)
# Kopieren nach .env.local fuer lokale Entwicklung.
NEXT_PUBLIC_API_BASE=http://localhost:3001

# Pitch/Demo: Frontend pollt alle 5s statt 30s
NEXT_PUBLIC_DEMO_MODE=true
