# Basis-URL des crisis-monitor Backends (GET /api/events).
# Backend laeuft per Default auf Port 3001 (siehe backend/.env, PORT=3001).
# Zum Nutzen: in .env.local kopieren. Ohne erreichbares Backend bleibt der
# Mock-Fallback (data/events.ts) aktiv.
NEXT_PUBLIC_API_BASE=http://localhost:3001
